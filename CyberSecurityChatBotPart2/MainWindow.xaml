<Window x:Class="CyberSecurityChatBotPart2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Chatbot" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#202024">

    <Window.Resources>
        <!-- Modern Styles -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FF0078D4" Offset="0"/>
                        <GradientStop Color="#FF005A9E" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#FF1991DB" Offset="0"/>
                                <GradientStop Color="#FF106EB3" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FFDC3545" Offset="0"/>
                        <GradientStop Color="#FFB02A37" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Background" Value="#FF3C3C40"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF555558"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="126*"/>
            <RowDefinition Height="137*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF0078D4" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🔒" FontSize="28" Margin="0,0,10,0"/>
                    <TextBlock Text="Cybersecurity Awareness Chatbot" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="1" 
                        x:Name="btnVoiceGreeting"
                        Content="🔊 Voice Greeting" 
                        Style="{StaticResource ModernButton}"
                        Width="120"
                        Click="BtnVoiceGreeting_Click"/>
            </Grid>
        </Border>

        <!-- Chat History Area -->
        <Border Grid.Row="1" Margin="20,15,20,10" Background="#FF2D2D30" CornerRadius="8" Grid.RowSpan="2">
            <ScrollViewer x:Name="chatScrollViewer" 
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <RichTextBox x:Name="rtxtChatHistory" 
                             Background="#FF2D2D30" 
                             Foreground="White"
                             FontFamily="Consolas"
                             FontSize="11"
                             BorderThickness="0"
                             IsReadOnly="True"
                             Padding="10">
                    <RichTextBox.Resources>
                        <Style TargetType="{x:Type Paragraph}">
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </RichTextBox.Resources>
                </RichTextBox>
            </ScrollViewer>
        </Border>

        <!-- Input Area -->
        <Border Grid.Row="2" Background="#FF2D2D30" Margin="20,274,20,50" CornerRadius="8" Padding="10" Grid.RowSpan="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtUserInput" 
                         Style="{StaticResource ModernTextBox}"
                         KeyDown="TxtUserInput_KeyDown"
                         VerticalContentAlignment="Center"/>

                <Button Grid.Column="1" 
                        x:Name="btnSend"
                        Content="Send →" 
                        Style="{StaticResource ModernButton}"
                        Width="80"
                        Margin="10,0,5,0"
                        Click="BtnSend_Click"/>

                <Button Grid.Column="2" 
                        x:Name="btnClear"
                        Content="Clear" 
                        Style="{StaticResource DangerButton}"
                        Width="70"
                        Click="BtnClear_Click"/>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#FF252528" Padding="15,8" Margin="0,70,0,0" Grid.RowSpan="2">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="8" Height="8" Fill="LightGreen" Margin="0,0,8,0"/>
                <TextBlock Text="Online - Ready to help with cybersecurity topics" 
                           Foreground="#FF888888" 
                           FontSize="11"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>